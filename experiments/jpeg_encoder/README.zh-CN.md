# JPEG encoder 实验

`evolve.json` 是归档的跨 design 实验记录：它记录导入 parent 与预期设置，但不是 AE 启动 profile；使用前必须重新提供 benchmark 输入并替换旧 runtime 路径。未来固定 JPEG artifact 应遵循 AES 的布局，写入 `artifact_evaluation/expected/jpeg_encoder/` 与 `artifact_evaluation/lineage/jpeg_encoder/`。
